// nino_behavior_wealth.v1 — reexport de contrato.
// A implementação vive em `../agent/behaviorWealth.ts` (pasta já empacotada por
// todas as Edge Functions dependentes). Este arquivo preserva o caminho
// canônico do contrato para testes e documentação.
export * from "../agent/behaviorWealth.ts";
